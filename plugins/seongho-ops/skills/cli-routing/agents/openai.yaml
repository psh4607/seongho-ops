interface:
  display_name: "CLI Routing"
  short_description: "Route developer and operations work to the right local CLI"
  default_prompt: "Use $seongho-ops:cli-routing to select, verify, and safely run the correct local CLI for this task."

policy:
  allow_implicit_invocation: true
